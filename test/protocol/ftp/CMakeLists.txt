if(TARGET FtpProto)
    add_executable(ftp_server test_ftp_server.cpp)
    add_executable(ftp_client test_ftp_client.cpp)
    add_executable(ftp_commands test_ftp_commands.cpp)
    add_executable(ftp_e2e test_ftp_e2e.cpp)
    add_executable(ftp_e2e_server test_ftp_e2e_server.cpp)
    add_executable(ftp_server_with_client test_ftp_server_with_client.cpp)
    add_executable(ftp_e2e_client test_ftp_e2e_client.cpp)
    add_executable(ftp_active_e2e test_ftp_active_e2e.cpp)
    add_executable(ftp_client_active_mode test_ftp_client_active_mode.cpp)
    add_executable(ftp_interactive test_ftp_interactive.cpp)
    add_executable(ftp_interactive_improved test_ftp_interactive_improved.cpp)
    add_executable(ftp_port_command test_ftp_port_command.cpp)
    add_executable(ftp_epsv_eprt test_ftp_epsv_eprt.cpp)
    add_executable(ftp_auto_fallback test_ftp_auto_fallback.cpp)
    add_executable(ftp_ipv6 test_ftp_ipv6.cpp)
    add_executable(test_fix_verification test_fix_verification.cpp)

    target_link_libraries(ftp_server PUBLIC FtpProto ServerServiceFtp)
    target_link_libraries(ftp_client PUBLIC FtpProto)
    target_link_libraries(ftp_commands PUBLIC FtpProto)
    target_link_libraries(ftp_e2e PUBLIC FtpProto)
    target_link_libraries(ftp_e2e_server PUBLIC FtpProto)
    target_link_libraries(ftp_e2e_client PUBLIC FtpProto)
    target_link_libraries(ftp_active_e2e PUBLIC FtpProto)
    target_link_libraries(ftp_client_active_mode PUBLIC FtpProto)
    target_link_libraries(ftp_server_with_client PUBLIC FtpProto)
    target_link_libraries(ftp_interactive PUBLIC FtpProto)
    target_link_libraries(ftp_interactive_improved PUBLIC FtpProto)
    target_link_libraries(ftp_port_command PUBLIC FtpProto)
    target_link_libraries(ftp_epsv_eprt PUBLIC FtpProto)
    target_link_libraries(ftp_auto_fallback PUBLIC FtpProto)
    target_link_libraries(ftp_ipv6 PUBLIC FtpProto)
    target_link_libraries(test_fix_verification PUBLIC FtpProto)

    if(BUILD_TESTING)
        add_test(NAME ftp_commands COMMAND ftp_commands)
        add_test(NAME ftp_port_command COMMAND ftp_port_command)
        add_test(NAME ftp_epsv_eprt COMMAND ftp_epsv_eprt)
        add_test(NAME ftp_e2e COMMAND ftp_e2e)
        add_test(NAME ftp_active_e2e COMMAND ftp_active_e2e)
        add_test(NAME ftp_client_active_mode COMMAND ftp_client_active_mode)
        add_test(NAME ftp_auto_fallback COMMAND ftp_auto_fallback)
        add_test(NAME ftp_ipv6 COMMAND ftp_ipv6)
        add_test(NAME ftp_fix_verification COMMAND test_fix_verification)
    endif()
endif()
